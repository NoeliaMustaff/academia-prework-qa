npm init playwright@latest (crear un proyecto nuevo de Playwright)

npx playwright test (ejecutar todos los tests)

npx playwright test --headed (ejecutar tests mostrando el navegador)

npx playwright test --ui (abrir interfaz visual para ejecutar tests)

npx playwright test --debug (ejecutar tests en modo debug paso a paso)

npx playwright test tests/archivo.spec.js (ejecutar un archivo de test específico)

npx playwright test --grep "texto" (ejecutar tests que coincidan con ese nombre)

npx playwright test --last-failed (re ejecutar solo los tests que fallaron)

npx playwright codegen https://sitio.com (grabar un flujo de usuario automáticamente)

npx playwright show-report (abrir el reporte HTML de los tests)

npx playwright install (instalar navegadores de Playwright)

npx playwright install --force (reinstalar navegadores)

npx playwright test --help (mostrar ayuda de comandos)

cd /d "D:\QA - CYS\academia-prework-qa\playwright"